<p>
    <?= Yii::t('DevtoolsModule.views_showcase_simple_remote_modal', 'Simple modal loading does not require an additional javascript handler. '
            . 'The ajax request is done by the action <code>ui.modal.load</code> which loads the dialog returned by data-action-url into the #globalModal.') ?>
</p>

